{{nav-bar}}
<!-- Page Content -->
<div class="container">
  <br>

  <div class="row">
    <div class="col-lg-12">
      <center><h1 class="page-header">XKCD FORUM</h1></center>
    </div>
  </div>

  <div class="row">
  {{#each model as |comic index|}}
    <div class="col-md-4">
      <div class="comic-template">
        <img class=" comic-image" src="{{comic.img}}" alt="">
        <h3>
          <a href="/{{comic.num}}">{{comic.title}}</a>
        </h3>
      </div>
    </div>
  {{/each}}
  </div>

  {{footer-comp}}
</div>
<!-- /.container -->
{{outlet}}
